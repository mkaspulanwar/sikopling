# Supabase public client (required for app runtime)
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=

# Supabase server secret (required for privileged server tasks only)
SUPABASE_SERVICE_ROLE_KEY=

# Optional local CLI helper (do not expose in frontend)
SUPABASE_DB_PASSWORD=
